flows:
  - name: default
    description: "Flujo de procesamiento con JavaScript"
    inflow:
      #- name: checkJWT
      #  type: com.example.integrationflow.impl.CheckOauth2
      #  description: "Verificacion de OAuth2"
      #- name: validate
      #  type: com.example.integrationflow.impl.JavaScript
      #  description: "Validacion de tramas"
      #  properties:
      #    code: |
      #      if (payload) {
      #        payload.toUpperCase() + " procesado";
      #      } else {
      #        "Payload nulo"; // O un mensaje de error apropiado
      #      }
      - name: validate
        type: PythonScript
        description: "Validacion de tramas"
        properties:
          code: |
            def process_payload(payload):
              return payload.upper() + " procesado"
            process_payload(payload)
    outflow: []
    exception: []